interface:
  display_name: "Write Task Spec"
  short_description: "Создаёт, проверяет и при политике доставляет spec"
  default_prompt: "Use $write-task-spec to create an implementation-ready task specification and, when configured and authorized, deliver only its exact documentation package through the documentation fast path."
