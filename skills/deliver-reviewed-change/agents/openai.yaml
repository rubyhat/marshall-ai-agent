interface:
  display_name: "Deliver Reviewed Change"
  short_description: "Проверяет и доставляет точные изменения"
  default_prompt: "Use $deliver-reviewed-change to deliver the exact current task through the configured reviewed flow or eligible documentation-only fast path."
